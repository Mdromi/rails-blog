<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="turbo-visit-control" content="reload">
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= turbo_include_tags %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="font-sans antialiased">
    <div class="container w-3/4 mx-auto p-4">
      <!-- Logo Section -->
      <div class="mb-8 text-center logo-container">
        <div class="logo"></div>
      </div>
      <!-- Menu Section -->
      <% unless controller_name == 'blog_posts' && action_name == 'show' %>
        <nav class="mb-8 flex justify-center items-center">
          <!-- Your Navigation Menu Goes Here -->
          <ul class="flex justify-center space-x-4 bg-none border-none">
            <li><%= link_to "Everything", root_path, class: "nav-link" %></li>
            <li><%= link_to "Releases", root_path, class: "nav-link" %></li>
            <li><%= link_to "News", root_path, class: "nav-link" %></li>
            <!-- Add more menu items as needed -->
          </ul>
          <!-- Logout Button and New Blog Post -->
          <div class="flex justify-center items-center ml-4 text-xl font-semibold">
            <% if user_signed_in? %>
              <%= link_to "New Post", new_blog_post_path, class: "bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 focus:outline-none focus:shadow-outline-blue active:bg-blue-800" %>
              <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "ml-4 bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 focus:outline-none focus:shadow-outline-red active:bg-red-800" %>
            <% end %>
          </div>
        </nav>
      <% end %>
      <!-- Content Section -->
      <div class="mb-8">
        <%= yield %>
      </div>
    </div>
    <!-- Footer Section -->
    <footer class="bg-[#F0E7E9] w-full py-4 text-center footer">
      <!-- Your Footer Content Goes Here -->
      <div class="footer-logo">
        <a href="/" aria-label="Ruby on Rails" class=""></a>
      </div>
    </footer>
  </body>
</html>
